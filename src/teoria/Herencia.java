package teoria;

public class Herencia {
    /**
     * En la herencia, la clase que hereda se le llama "superclase",
     * mientras que la clase que recibe la herencia es denominada como
     * "subclase".
     *
     * Pueden haber mas de 2 niveles de herencia, pero se recomienda
     * no utilizar mas de 3 niveles.
     */

    /*
    ======================
    = REGLAS DE HERENCIA =
    ======================

    -Se puede cambiar una variable heredada.
    -La subclase utiliza la palabra reservada 'extends'.
    -La superclase es abstracta.
     */
}
